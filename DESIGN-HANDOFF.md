# 디자인 인계 문서 — AI 피트니스 & 식단 플래너

> 이 문서는 **디자인을 맡을 AI가 읽는 것**을 전제로 씁니다.
> 코드를 고치기 전에 "이 앱이 무엇이고, 스타일이 어디에 어떻게 들어 있고,
> 무엇을 건드리면 기능이 깨지는지"를 먼저 파악할 수 있게 정리했습니다.

---

## 1. 제품 요약

설문 한 번으로 **일주일 운동 루틴과 식단**을 만들고, 매일의 기록을 읽어
다음 주 루틴을 다시 짜는 웹 앱입니다. 한국어 UI이고, 개인 사용자가 혼자 씁니다.

추천은 AI에 통째로 맡기지 않고 규칙 엔진이 판단합니다. AI는 사진 분석과
자유 검색 등 네 자리에만 씁니다.

---

## 2. 기술 스택과 제약 — **반드시 지킬 것**

| 항목 | 내용 |
|---|---|
| 프레임워크 | React 19 + Vite |
| 스타일 | **순수 CSS 파일 2개.** Tailwind · CSS-in-JS · UI 라이브러리 **없음** |
| CSS 규모 | `App.css` 4,185줄 + `index.css` 53줄 |
| 클래스 네이밍 | BEM 아님. `kebab-case` 전역 클래스 (`.plan-row`, `.meal-chip`) |
| CSS Modules | 쓰지 않음 — **모든 클래스가 전역**이라 이름 충돌에 주의 |
| 아이콘 | 이모지 문자 (`📅 ✏️ 📊 🏅 📖 🎧`). 아이콘 폰트·SVG 세트 없음 |
| 폰트 | `system-ui` 스택. 웹폰트 없음 |
| 다크 모드 | **없음.** `prefers-color-scheme` 사용처 0곳 |
| 언어 | UI 전부 한국어. 조사 처리 유틸(`withParticle`)이 있으니 문구 변경 시 주의 |

> **새 라이브러리를 넣지 말아 주세요.** Tailwind 도입이나 CSS-in-JS 전환은
> 4,000줄을 전부 옮기는 작업이라 별도 합의가 필요합니다.
> 지금은 `App.css`를 고쳐서 디자인을 바꾸는 것을 전제로 합니다.

---

## 3. 파일 지도

```
fitness-app/src/
├── App.jsx                    화면 전환 + 탭 + 헤더 (216줄)
├── styles/
│   ├── index.css              리셋, 기본 타이포 (53줄)
│   └── App.css                앱 전체 스타일 (4,185줄) ← 작업 대상
├── components/
│   ├── Onboarding.jsx         설문 10단계          (617줄)
│   ├── RoutineRecommendation.jsx  루틴 후보 3안    (166줄)
│   ├── Dashboard.jsx          주간 계획 + 식단      (693줄)
│   ├── LogPanel.jsx           달력 + 운동/식단 기록 (718줄)
│   ├── WeeklyReport.jsx       주간 분석            (288줄)
│   ├── Achievements.jsx       포인트·뱃지·성장     (191줄)
│   ├── ExerciseGuide.jsx      운동 도감 + 검색      (305줄)
│   ├── MusicPanel.jsx         플레이리스트         (316줄)
│   ├── MealForm.jsx           식단 입력 + 사진      (230줄)
│   ├── PlanSwitcher.jsx       플랜 갈아타기         (98줄)
│   ├── AuthPanel.jsx          로그인 메뉴          (107줄)
│   ├── MuscleMap.jsx          자극 부위 인체도      (121줄)
│   ├── GrowthBody.jsx         성장 아바타          (107줄)
│   ├── BmiScale.jsx           BMI 눈금             (61줄)
│   ├── LogCalendar.jsx        달력                 (168줄)
│   └── SafetyNotice.jsx       안전 경고            (44줄)
└── data/
    ├── muscles.js             부위 색·이름 매핑
    └── muscleFigure.js        인체도 좌표 (순수 데이터)
```

---

## 4. 화면 구조

### 4.1 최상위 뷰 — 한 번에 하나만 보임

`App.jsx`의 `currentView` 상태가 결정합니다.

```
loading  →  onboarding  →  routine-recommendation  →  dashboard
                  ↓
               blocked        (안전 문항에서 block 등급이 나온 경우)
```

| 뷰 | 화면 |
|---|---|
| `loading` | "저장된 계획을 불러오는 중입니다" |
| `onboarding` | 설문 10단계. 상단에 진행 막대 |
| `blocked` | 의료진 상담 안내. 계획을 만들지 않음 |
| `routine-recommendation` | 루틴 후보 3안 카드 → 하나 선택 |
| `dashboard` | 본 화면. 여기서만 하단 탭 바가 나옴 |

### 4.2 공통 레이아웃

```
┌─ .app-shell (max-width 1120px, 가운데 정렬) ──────────────┐
│ ┌─ .app-header ───────────────────────────────────────┐ │
│ │ [로고+제목]   [설문·추천·관리 스텝퍼]   [.auth-panel] │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                          │
│   (현재 뷰의 내용)                                        │
│                                                          │
└──────────────────────────────────────────────────────────┘
┌─ .tab-bar ── position:fixed, bottom:0, 6칸 균등분할 ─────┐
│  📅계획  ✏️기록  📊리포트  🏅성장  📖운동  🎧음악        │
└──────────────────────────────────────────────────────────┘
```

- `.tab-bar`는 **화면 하단 고정**. 반투명 흰 배경 + `backdrop-filter: blur(8px)`
- `env(safe-area-inset-bottom)`으로 아이폰 홈 인디케이터 대응
- `.app-shell`의 하단 패딩이 탭 바 높이(`--tabbar-h: 64px`)를 비켜 감

### 4.3 대시보드 6개 탭

| 탭 | 컴포넌트 | 주요 구성 |
|---|---|---|
| 📅 계획 | `Dashboard` | 활성 플랜 헤더 + `PlanSwitcher` / 주차 이동 / 요일 7줄(운동·식단) / 치트데이 폼 / AI 코칭 |
| ✏️ 기록 | `LogPanel` | 월 달력 → 날짜 선택 → 운동 세트 표 + `MealForm` + 그날 식단 목록 |
| 📊 리포트 | `WeeklyReport` | 수행률 막대 / 운동별 최고 기록 / 영양 달성률 / 다음 주 재생성 |
| 🏅 성장 | `Achievements` | 레벨 카드 / **성장 아바타**(`GrowthBody`) / 지표 4칸 / 뱃지 그리드 / 최근 적립 |
| 📖 운동 | `ExerciseGuide` | 검색창 / 동작 목록 / 상세(**인체도 `MuscleMap`** + 수행법 + 영상) |
| 🎧 음악 | `MusicPanel` | 장르 8칩 × 길이 4칩 → 검색 링크 목록 |

---

## 5. 현재 디자인 토큰

`App.css` 최상단 `:root`에 **10개뿐**입니다.

```css
:root {
  --page-pad: 16px;      /* 640px↑ 24px, 960px↑ 20px 로 변경됨 */
  --gap: 14px;           /* 640px↑ 16px, 960px↑ 18px */
  --radius: 12px;
  --tap: 46px;           /* 최소 터치 영역 */
  --tabbar-h: 64px;

  --line: #e2e8f0;       /* 테두리 */
  --ink: #0f172a;        /* 본문 진한 글자 */
  --muted: #64748b;      /* 보조 글자 */
  --surface: #ffffff;    /* 카드 배경 */
  --surface-2: #f8fafc;  /* 한 단계 눌린 배경 */
  --brand: #2563eb;      /* 강조 파랑 */
}
```

> **알려진 약점 — 개선 여지가 큰 부분입니다.**
> 위 10개를 뺀 나머지 색이 **전부 하드코딩된 hex**입니다
> (`#eff6ff`, `#b91c1c`, `#166534` … 수백 곳).
> 토큰 체계를 세우고 하드코딩 색을 걷어내는 작업은 환영합니다.
> 다만 **§7의 의미 있는 색**은 예외입니다.

### 브레이크포인트

| 쿼리 | 쓰임 |
|---|---|
| `min-width: 640px` | 주력 분기점. 1열 → 2열, 패딩 확대 (9곳) |
| `min-width: 720px` | 음악 패널 (1곳) |
| `min-width: 960px` | 데스크톱 여백 (1곳) |
| `max-width: 480px` | 작은 폰 보정 (2곳) |

**모바일 우선**으로 작성돼 있습니다. 기본 스타일이 좁은 화면 기준입니다.

---

## 6. 공통 클래스 어휘

전역 클래스라 이름이 곧 계약입니다. 자주 쓰는 것들:

| 클래스 | 쓰임 | 사용처 |
|---|---|---|
| `.panel` | 흰 카드 컨테이너. 화면의 기본 단위 | 38곳 |
| `.section-heading` | 카드 상단 제목+설명 묶음 | 13곳 |
| `.eyebrow` | 제목 위 작은 대문자 라벨 | 16곳 |
| `.primary-button` / `.secondary-button` | 버튼 2종 | 26곳 |
| `.metric` | 숫자 타일 (지표 그리드 칸) | 15곳 |
| `.tag` / `.chip` | 알약 배지 / 선택 칩 | 21곳 |
| `.loading-state` | "불러오는 중입니다" | 11곳 |
| `.empty-note` | "기록이 없습니다" | 6곳 |
| `.form-error` | 오류 문구 | 12곳 |

이름을 바꾸려면 **JSX와 CSS를 함께** 고쳐야 합니다. CSS만 고치면 조용히 스타일이 빠집니다.

---

## 7. 건드리면 기능이 깨지는 것 — **중요**

### 7.1 JS가 값을 넣는 인라인 스타일

CSS로 덮어쓰면 동작이 깨집니다. **셀렉터를 더 강하게 만들지 마세요.**

| 위치 | 하는 일 |
|---|---|
| `Achievements.jsx`, `WeeklyReport.jsx`, `LogPanel.jsx`, `Onboarding.jsx` | 진행 막대 `style={{ width: "42%" }}` |
| `BmiScale.jsx` | BMI 구간 색과 폭, 현재 위치 마커 `left` |
| `MuscleMap.jsx` | 근육마다 `style={{ "--tone": "#e2574c" }}` |
| `GrowthBody.jsx` | 근육 진하기 `style={{ fillOpacity: 0.63 }}` |

### 7.2 부위 색 팔레트 — 세 화면이 공유

`.tag.m-chest`, `.m-back`, `.m-legs` … 9종 (`data/muscles.js`의 `muscleClass()`가 붙임).
**계획 · 달력 · 리포트가 같은 색을 씁니다.** 한 곳만 바꾸면 세 화면의 의미가 어긋납니다.

### 7.3 인체도 SVG — 색이 곧 정보

`MuscleMap`(운동 탭)과 `GrowthBody`(성장 탭)는 손으로 그린 SVG입니다.

```
색   = 어느 근육인가        (data/muscleFigure.js 의 REGION_COLOR 26종)
진하기 = 그 운동이 쓰는가     (주동근 1.0 / 협응근 0.85 / 그 외 0.05)
```

- 이 두 축은 **장식이 아니라 정보**입니다. 임의로 통일하지 마세요.
- 좌표(`muscleFigure.js`)는 근육이 몸 밖으로 새지 않게 맞춰 둔 값입니다. **건드리지 마세요.**
- 바꿔도 되는 것: 실루엣 살색(`.body-base`), 윤곽선(`.body-outline`), 카드 테두리, 캡션 배지(`.view-chip`)

### 7.4 상태를 나타내는 색

의미가 붙어 있어 임의 변경 시 오해를 부릅니다.

| 색 계열 | 뜻 | 예 |
|---|---|---|
| 빨강 | 위험·차단·부족 | 안전 `block`, `.tag.gear.missing`(기구 없음) |
| 노랑/주황 | 주의 | 안전 `warn`, RPE 높음 |
| 초록 | 좋음·달성 | 목표 달성, `.tag.home`(집에서 가능) |
| 파랑(`--brand`) | 강조·선택됨 | 활성 탭, 선택된 칩 |

---

## 8. 디자인이 특히 아쉬운 곳 (우선순위 제안)

1. **토큰 부재** — 색이 하드코딩. 팔레트·간격·타이포 스케일 정립이 가장 큰 개선
2. **타이포그래피** — `system-ui` 한 벌에 크기만 다름. 위계가 약함
3. **다크 모드 없음** — 운동 앱은 야간 사용이 많아 수요가 있음
4. **밀도** — 카드가 38개 `.panel`로 균일해서 무엇이 중요한지 안 읽힘
5. **빈 상태·로딩** — 텍스트 한 줄뿐. 일러스트나 스켈레톤 여지
6. **이모지 아이콘** — 탭 바가 OS마다 다르게 보임. 일관된 아이콘 세트 여지

---

## 9. 작업 방법

```bash
cd fitness-app && npm install
npm run dev -- --port 5173     # 프론트

cd fitness-backend-js && npm start   # 백엔드 (:8003) — 화면 확인에 필요
```

- 스타일 변경은 **`src/styles/App.css`**에서. 구조가 바뀌어야 하면 해당 `.jsx`
- 검증: `cd fitness-app && npx eslint src/`
- 클래스 이름을 바꿀 때는 `grep -rn "클래스명" src/`로 사용처를 먼저 확인

### 확인해야 할 화면 폭

`375px`(모바일) · `640px`(분기점 직후) · `960px`(데스크톱) 세 가지에서
가로 스크롤이 생기지 않는지 봐 주세요. 하단 탭 바가 콘텐츠를 가리지 않는지도요.
